import React from "react";

const Header = () => {
  return (
    <header style={{ padding: "15px", background: "#0f172a", color: "white" }}>
      <h2>✈️ AI Trip Planner</h2>
    </header>
  );
};

export default Header;
